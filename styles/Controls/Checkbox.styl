.fui-checkbox,
.fui-radio
	display: inline-block
	position: relative
	margin: $widgetMargin
	height: $checkBoxSize

	padding-left: $checkBoxSize + $checkBoxLabelMargin

	color: $widgetFontColor

	vertical-align: top

	font-family: $baseFont

	input[type="checkbox"],
	input[type="radio"],
	i
		position: absolute
		background: $widgetBackground

		margin-left: -1*$checkBoxSize - $checkBoxLabelMargin

	i
		box-sizing: border-box
		display: inline-block
		overflow: hidden; // We'll hide by moving out of the box
		width: $checkBoxSize
		height: $checkBoxSize

		color: $widgetFontColor
		text-align: center
		font-size: 14px
		font-style: normal
		line-height: 20px

		cursor: pointer

		padding-left: $checkBoxSize // Move contents out of box


	input[type="checkbox"],
	input[type="radio"]
		opacity: 0.0001

		&:checked + i
			padding-left: 0 // Show the contents again

	&.fui-primary
		&, &:active
			background: transparent

		i
			background: $widgetBackgroundPrimary

	input:focus + i
		width: $checkBoxSize + 2
		height: $checkBoxSize + 2

		margin: -1px
		margin-left: -1*$checkBoxSize - $checkBoxLabelMargin - 1

		glow()

.fui-checkbox
	i
		border-radius: 2px

		&:before
			@extend .fuicon-check:before

