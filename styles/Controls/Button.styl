.fui-button
	padding: 8px 12px

	text-align: center

	cursor: pointer // Hand on mouseover
	pointer-events: auto // Allow interactivity

	emboss()

	&:active
		background: $widgetBackgroundDown

	&:focus
		margin-left: $widgetMargin - 1
		margin-right: $widgetMargin - 1
